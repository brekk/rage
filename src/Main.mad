import { always, noop } from "Function"
import IO from "IO"
import { fulfill } from "Wish"

// import Ext from "@/External"
import {
  readConfigFile,
} from "@/ConfigFile"



main = () => {
  undertaker = fulfill(
    pipe(
      IO.pTrace("BAD!"),
      noop,
    ),
    pipe(
      show,
      IO.put,
      noop,
    ),
  )

  pipe(
    readConfigFile,
    undertaker,
  )("./example.rage.json")
}
