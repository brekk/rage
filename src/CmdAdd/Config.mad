import type { Maybe } from "Maybe"
import type { Wish } from "Wish"

import type { StatusFail, StatusPass } from "@/CmdAdd/Status"



export alias RageAddDef = {
  archivePattern :: String,
  domain :: Maybe String,
  help :: Boolean,
  repo :: Maybe String,
  save :: Boolean,
  tag :: Maybe String,
  useColor :: Boolean,
}

export alias RageAddState = Wish StatusFail StatusPass
